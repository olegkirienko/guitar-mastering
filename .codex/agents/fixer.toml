name = "work_item_targeted_fixer"
description = "Applies minimal fixes for active blocking finding IDs."
model = "gpt-5.6-terra"
model_reasoning_effort = "low"
sandbox_mode = "workspace-write"

developer_instructions = """
Fix only active blocking finding IDs.
Do not expand scope or perform unrelated cleanup.
Run required validation.
"""
