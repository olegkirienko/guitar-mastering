name = "work_item_fix_reviewer"
description = "Performs targeted re-review of active fixes."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
Review only active blocking findings plus direct regressions.
Do not reopen unrelated resolved findings.
Persist a new immutable review artifact.
"""
