version: 2

work_item_id: railway-ci-cd-iac
work_item_type: infrastructure
title: "Railway GitHub CI/CD and Infrastructure as Code"

phase: implementation
status: in_progress
gate: none

design:
  path: docs/technical-designs/railway-ci-cd-iac.md
  status: approved

amendments:
  - id: restart-policy-parity-01
    path: docs/reviews/railway-ci-cd-iac/design-amendment-01-restart-policy-parity.md
    status: approved
  - id: verifier-credential-02
    path: docs/reviews/railway-ci-cd-iac/design-amendment-02-verifier-credential.md
    status: approved
  - id: production-web-service-identity-03
    path: docs/reviews/railway-ci-cd-iac/design-amendment-03-production-web-service-identity.md
    status: approved

context:
  - AGENTS.md
  - .github/workflows/ci.yml
  - package.json
  - server/config.ts
  - server/app.ts
  - docs/operations/production-cutover.md
  - docs/operations/legacy-platform-retirement-record.md
  - docs/operations/railway-iac-parity-foundation-plan.md
  - docs/operations/github-main-validation-plan.md
  - docs/operations/railway-github-autodeploy-plan.md
  - docs/operations/railway-first-link-rehearsal-plan.md
  - docs/operations/railway-first-link-rehearsal-evidence-2026-09-25.md
  - docs/operations/railway-end-to-end-cicd-acceptance-plan.md
  - docs/operations/railway-end-to-end-cicd-acceptance-evidence-2026-09-26.md
  - docs/operations/railway-deployment-version-retirement-plan.md

completed_slices:
  - railway-iac-parity-foundation
  - github-main-validation
  - deployment-metadata-and-runbook
  - railway-github-autodeploy

current_slice:
  id: end-to-end-cicd-acceptance
  name: "End-to-end CI/CD acceptance"
  status: in_progress

latest_review:
  path: docs/reviews/railway-ci-cd-iac/fix-rereview-15-railway-github-autodeploy.md
  verdict: APPROVED

blocking_findings: []

next:
  phase: implementation
  action: implement-end-to-end-cicd-acceptance
  human_approval_required: false

notes:
  - "On 2026-09-26 the owner explicitly approved the current next_slice_approval gate for entry into the final end-to-end-cicd-acceptance slice. The approved railway-github-autodeploy slice is archived in completed_slices and acceptance implementation is now authorized only within the authoritative positive and protected-branch negative acceptance plan. Branch protection must not be weakened, production failure-path evidence remains mandatory, and unresolved acceptance evidence blocks completion."
  - "Fix Re-Review 15 verifies HIGH-04 and MEDIUM-03 as fixed and approves railway-github-autodeploy. The Railway IaC source declaration preserves olegkirienko/guitar-mastering, main, and Wait for CI; a fresh authenticated Railway CLI 5.57.7 read-only plan under Node 24.7.0 returned 'Your Railway configuration is already up to date' with unchanged production identity and topology. The authoritative design and operator record explicitly preserve the failed first-link rehearsal and reconcile the owner-directed source stage/commit as a one-time deviation without claiming the original proof requirement was satisfied. Full validation passed: pnpm lint; pnpm test (73 passed, 19 skipped); pnpm test:browser (10 passed); pnpm test:postgres (19 passed); pnpm build; workflow-state assertions; and git diff --check. No Railway or GitHub mutation, apply, acceptance commit, push, migration, or deployment occurred. The later approved end-to-end-cicd-acceptance slice exists, so explicit next_slice_approval is required before it begins."
  - "Targeted fixes for HIGH-04 and MEDIUM-03 are complete and ready for fix re-review. .railway/railway.ts now represents the effective production source olegkirienko/guitar-mastering, branch main, and Wait for CI checkSuites true while preserving autodeploy behavior, verifier/pre-deploy settings, service identity, and topology. A fresh authenticated read-only Railway CLI 5.57.7 plan under Node 24.7.0 at 2026-09-26T11:22:33Z returned 'Your Railway configuration is already up to date' with no add, change, destroy, source, branch, Wait-for-CI, variable, command, identity, or topology mutation; no apply or other provider mutation followed. The authoritative design and operator record now explicitly reconcile that the approved first-link proof was not obtained: after the isolated rehearsal failed, the owner manually staged and committed the production source without redeploy as a one-time deviation; no source-link deployment was created; the exact-SHA predeploy verifier and previously proven fail-closed behavior were already active; and post-link read-only checks verified source, branch, Wait for CI, autodeploy, production health, and deployment history. This does not claim or retroactively satisfy the original first-link proof requirement. Node 24.7.0 validation passed: pnpm lint; pnpm test (73 passed, 19 skipped); pnpm test:browser (10 passed); pnpm test:postgres (19 passed); pnpm build; and git diff --check. HIGH-04 and MEDIUM-03 remain active only pending targeted re-review. No remote Railway mutation, acceptance commit, push, migration, or deployment occurred, and end-to-end-cicd-acceptance remains unauthorized."
  - "Implementation Review 14 requires targeted fixes for HIGH-04 and MEDIUM-03. HIGH-04: the effective production GitHub source and Wait for CI setting are absent from .railway/railway.ts, so a fresh Node 24.7.0 Railway plan proposes removing source.repo, source.type, and source.checkSuites; add exact IaC source parity and prove a no-change plan without applying it. MEDIUM-03: the isolated rehearsal failed to prove first-link ordering, but an owner-directed production checklist later connected source and enabled Wait for CI/autodeploy without the approved design's intervening amendment path; preserve the immutable evidence and reconcile the current design/operator/state record as a one-time deviation without inventing retroactive proof. Live source, trigger, autodeploy, deployment, and smoke checks otherwise passed. No provider mutation, commit, push, migration, or deployment occurred during review. The end-to-end-cicd-acceptance slice remains unauthorized."
  - "The railway-github-autodeploy slice is implemented and ready for implementation review. Final read-only Railway CLI 5.57.7 and GraphQL verification completed at 2026-09-26T11:06:58Z against fixed project 112644ba-cb91-443b-ae4b-73a0d6f74b69, production environment 994fd373-dd1d-4073-8b7f-116e77d898fa, and web service 4d0a3739-0beb-4ea9-9a7e-7a9f3494708e. Effective source is olegkirienko/guitar-mastering with main trigger 37dfea95-f342-49fa-af20-08dfbb9d3607; Wait for CI reports checkSuites true with one valid suite; Autodeploy is enabled; watch paths and the staged patch are empty. No source-link deployment was created: failed guard-test deployment 56a63457-6062-4083-8c06-d2422e4ea45e remains newest, while deployment 461b23f8-9b06-4033-b225-f46571b5d350 remains active SUCCESS with a running instance. Health, readiness, and root returned 200 and the unknown API route returned 404. Effective future config retains pnpm release:predeploy, GITHUB_CI_GATE_REQUIRED=true, the sealed credential variable name, and the previously image-proved verifier artifact. The active bootstrap instance retains its earlier false process snapshot because the true-gate CLI deployment failed closed and never promoted; the effective service value for the first GitHub-triggered deployment is true. No provider mutation or acceptance commit was performed. Evidence is recorded in docs/operations/railway-github-autodeploy-plan.md. The later approved end-to-end-cicd-acceptance slice exists, but it remains blocked pending an APPROVED implementation review and explicit next_slice_approval."
  - "On 2026-09-25 the owner reported manually connecting olegkirienko/guitar-mastering main to the production web service with no further Railway setting change. Read-only Railway CLI 5.57.7 and live GraphQL inspection completed at 2026-09-25T10:09:12Z against fixed project 112644ba-cb91-443b-ae4b-73a0d6f74b69, production environment 994fd373-dd1d-4073-8b7f-116e77d898fa, and web service 4d0a3739-0beb-4ea9-9a7e-7a9f3494708e. Effective source remained repo null/image null; service repo triggers and production deployment triggers were empty; autodeploy remained enabled false, canEnable false, reason NO_REPO; and no unmerged change count existed. No deployment was created: newest history remained failed bootstrap deployment 56a63457-6062-4083-8c06-d2422e4ea45e from 09:03:05.931Z, while active deployment 461b23f8-9b06-4033-b225-f46571b5d350 remained SUCCESS and running. The reported dashboard action did not become effective on the fixed production service. Wait for CI was not enabled, Autodeploy was not changed, no provider mutation was performed during inspection, and no acceptance commit was created. The next allowed action is a manual retry only after re-confirming the exact production environment and service in the dashboard, followed immediately by the same read-only inspection."
  - "On 2026-09-25 the remaining production source-link step was simplified, by explicit owner direction, to one manual fail-closed operator checklist in docs/operations/railway-github-autodeploy-plan.md. It requires live production identity and health checks, Autodeploy OFF before manually connecting olegkirienko/guitar-mastering main to the existing web service, and an immediate deployment-history inspection. Any deployment created by the connection is an unconditional stop: report its ID and exact status without enabling Wait for CI or Autodeploy. Only when no deployment is created may the operator enable and read back Wait for CI, then Autodeploy, verify effective source/branch/settings and unchanged identities/health, and stop before any acceptance commit. The exact-SHA predeploy verifier and all fail-closed behavior remain unchanged. No provider mutation, commit, push, rehearsal plan, design amendment, or design-review cycle was performed by this documentation update; the slice remains in implementation pending manual execution."
  - "The explicitly approved isolated first-link rehearsal ran on 2026-09-25 and failed closed before source connection: disposable autodeploy was safely written and read back as disabled before any deployment, but the single serviceConnect call returned BAD_USER_INPUT 'User does not have access to the repo' (trace 4118968244649701621). Source remained null, no deployment trigger existed, hasEverDeployed remained false, and no deployment ID or timestamp was created. Wait for CI and autodeploy enable were not attempted, so no deterministic production first-link sequence was proved and the production operator plan was not updated. Disposable project a05b32a4-f354-4de2-aadb-315990f23b1b, environment 4201ac3a-63bd-4368-b62e-605a50c0cdc7, and service 26b137b9-4601-43bf-b893-ff822e1cc7d7 were deleted; workspace read-back found no live rehearsal resource. Production source remains unset and no production mutation occurred. Evidence: docs/operations/railway-first-link-rehearsal-evidence-2026-09-25.md. The next allowed action is a narrowly reviewed design amendment addressing repository access before any retry; production source, Wait for CI, and autodeploy remain blocked."
  - "Earlier on 2026-09-25 a minimal isolated first-link rehearsal plan was prepared at docs/operations/railway-first-link-rehearsal-plan.md. It specified one fresh disposable project with one empty service, hard-denied all production IDs, pre-disabled autodeploy, measured source/Wait-for-CI/autodeploy/deployment ordering with deployment IDs and UTC timestamps, and required verified whole-project cleanup. At preparation time the rehearsal had not run and required explicit approval; the later execution outcome is recorded in the newer note above and its immutable evidence artifact. Production source, Wait for CI, autodeploy, variables, IaC, and services remained unchanged and blocked."
  - "The ordered production-image guard bootstrap is complete. The owner-staged token patch 75d6d102-9375-41df-a0dc-91a22b9b2a11 was committed with deploys skipped; read-back proves GITHUB_ACTIONS_READ_TOKEN present and sealed. The reviewed gate-false/pre-deploy saved plan caused deployment 461b23f8-9b06-4033-b225-f46571b5d350, which reached SUCCESS with pnpm release:predeploy, explicit bypass before migration, clean smoke, and unchanged topology/source. Image-local verification on instance a3a7ff2f-ebd0-440a-9b7f-04bf1f9e56a2 passed main SHA 7e8a3eb88246c3c5fdc676b9d5f18347e9a6098f against run 35979509606/job 107567779717 and rejected an impossible SHA with exit 1. The single-variable gate-true saved plan caused CLI deployment 56a63457-6062-4083-8c06-d2422e4ea45e to fail on missing RAILWAY_GIT_COMMIT_SHA before any actual migration; prior deployment 461b23f8-9b06-4033-b225-f46571b5d350 remains active, SUCCESS, and smoke-clean. Final IaC plan is clean; token sealed; gate true; source unset; identities unchanged. Source connection, Wait for CI, and autodeploy remain blocked pending first-link proof and a separate explicit source-setting approval; the next allowed action is proof acquisition or a design amendment, not provider source mutation."
  - "The approved bootstrap has completed its protected-branch and image stages. PR 5 merged as main SHA 7e8a3eb88246c3c5fdc676b9d5f18347e9a6098f after PR validation; push Validate run 35979509606/job 107567779717 then passed for that exact SHA. CLI deployment 063aef0c-bff0-4587-9eb7-0328dc56c729 reached SUCCESS under the old pnpm db:migrate command, production smoke passed, and read-only SSH proved dist-server/verify-ci.js plus the exact release:predeploy script in the deployed image. GITHUB_ACTIONS_READ_TOKEN is absent locally and in the image, so implementation is paused before the owner-only credential creation/installation boundary. No source, Wait for CI, autodeploy, IaC, variable, database, domain, volume, or PITR mutation followed."
  - "On 2026-09-24 the owner explicitly approved the fresh Design Amendment 03 design_approval gate. The railway-github-autodeploy slice resumes in implementation with the corrected production web-service identity. Work remains limited to the already approved design and reviewed bootstrap boundaries; production source connection, Wait for CI, and autodeploy remain separately blocked pending first-link proof and a new explicit source-setting approval."
  - "Design Review 13 verifies LOW-01 as resolved and approves revised Design Amendment 03 with no active findings. The amendment accurately records both historical UUID entries, does not claim the immutable evidence is internally consistent, and bases the correction on the live Railway identity, later verified full-UUID evidence, and corrected current operator documents. The railway-github-autodeploy slice remains paused at a fresh design_approval gate; no prior approval is reusable, and no implementation or provider mutation is authorized by the review."
  - "The design phase addressed Design Review 12 LOW-01 only. Amendment 03 now states that immutable 2026-09-17 cutover evidence contains both an earlier truncated web-service UUID and a later full verified UUID, without claiming internal consistency. It bases the authoritative correction on the current live Railway identity, the later verified full-UUID evidence, and the corrected current bootstrap plan and CI/CD runbook. Historical evidence, bootstrap sequencing, IaC, application code, GitHub state, and Railway provider state were unchanged. LOW-01 remains active pending a new immutable design review."
  - "Design Review 12 finds LOW-01 unresolved: the immutable 2026-09-17 cutover evidence contains the truncated web-service UUID in its early topology summary and the full verified UUID in its later Railway read-back, while revised Amendment 03 inaccurately says the evidence already agrees without qualification. Revise only the amendment's audit statement and trail to record both historical references; keep the evidence immutable. The railway-github-autodeploy slice remains paused, no human approval is consumable, and no implementation or provider mutation is authorized."
  - "The design phase addressed Design Review 11 LOW-01 only: Amendment 03 now states accurately that the immutable 2026-09-17 cutover evidence already contains the verified full web-service UUID. The historical evidence and application code were not changed. The revised amendment is ready for a new immutable design review; LOW-01 remains active until that review resolves it, and no provider mutation or human approval is authorized."
  - "Design Review 11 requires the LOW-01 design-only correction: Amendment 03 inaccurately says the immutable 2026-09-17 cutover evidence contains the truncated web-service identity, but that artifact already contains the full correct UUID. Revise only that audit statement and submit a new immutable design review. The railway-github-autodeploy slice remains paused; no human approval is currently consumable, and no implementation or provider mutation is authorized."
  - "On 2026-09-24 a read-only production preflight confirmed that the bootstrap plan and CI/CD runbook contained a truncated web-service UUID. Design Amendment 03 corrects only those current references to live service 4d0a3739-0beb-4ea9-9a7e-7a9f3494708e. The earlier bootstrap approval was not consumed and cannot be reused; after immutable design review, a new explicit design approval is required before implementation or any bootstrap/provider mutation resumes. No remote state changed."
  - "The railway-github-autodeploy repository verifier and its 14-test focused suite are implemented. Node 24.7.0 validation passed: pnpm lint; pnpm test (73 passed, 19 skipped); pnpm test:browser (10 passed); pnpm test:postgres (19 passed); pnpm build; and git diff --check. The initial sandboxed unit run could not bind localhost (EPERM); the unrestricted rerun passed without code changes. The final built verifier passed an authenticated read-only live check for main SHA dda15469df039e3cbb349d6b3a02f2b015988665, workflow run 35590758279, and job 106304363143 without printing or persisting the credential. GitHub exposes only Validate/validate on current main. Railway production identities and the healthy deployment were confirmed read-only, and the web source remains unset. The reviewed bootstrap plan is docs/operations/railway-github-autodeploy-plan.md; explicit approval is required before any CLI deployment, credential installation, Railway variable/IaC mutation, or SSH proof. Production source connection, Wait for CI, and autodeploy remain blocked because current CLI/public documentation does not prove safe first-link ordering."
  - "On 2026-09-22 the owner explicitly approved the Design Amendment 02 design_approval gate. The railway-github-autodeploy slice resumes under the approved verifier-credential design. Implementation is limited to that slice and amendment; credential creation and Railway variable/IaC/source/autodeploy mutations still require the design's separately reviewed operator actions."
  - "Design Review 10 approves Design Amendment 02 with no findings. The authenticated verifier mode uses a repository-only fine-grained Actions: read token, no anonymous fallback, bounded API requests, fail-closed rate-limit/error handling, sealed storage, and reviewed rotation/revocation. The railway-github-autodeploy slice remains paused at design_approval; this review authorizes no implementation, credential, Railway variable/IaC/source/autodeploy change, deployment, migration, or other provider mutation."
  - "On 2026-09-22 the owner explicitly approved next_slice_approval only to enter railway-github-autodeploy. The approved deployment-metadata-and-runbook slice was archived in completed_slices and railway-github-autodeploy became current, but implementation is paused for the separately required verifier-credential amendment review and design_approval."
  - "Design Amendment 02 is submitted for immutable review. Read-only anonymous and authenticated GitHub API checks returned 200 for the pinned workflow-runs and jobs endpoints; anonymous access had a shared source-IP core budget of 60/hour while authenticated access had 5000/hour. The amendment selects a repository-only fine-grained Actions: read credential with no anonymous fallback, at-most-90-day expiry, sealed Railway storage, rate-limit/failure guards, and reviewed rotation/revocation. No credential, verifier code, IaC, GitHub setting, Railway variable/source/autodeploy setting, deployment, or other provider mutation was created or changed."
  - "Implementation Review 09 approves deployment-metadata-and-runbook with no findings. A later approved railway-github-autodeploy slice exists, so explicit next_slice_approval is required before it begins. That approval does not waive the separate narrow verifier-credential amendment, immutable design review, and explicit design approval required before verifier implementation or production activation. No provider command or remote mutation was executed during review."
  - "The deployment-metadata-and-runbook slice is implemented and ready for implementation review. Runtime configuration prefers a validated full RAILWAY_GIT_COMMIT_SHA while preserving required DEPLOYMENT_VERSION fallback behavior; contributor, cutover, IaC, SHA-correlation, drift, failure-response, and rollback guidance is updated. Node 24.7.0 validation passed: pnpm lint; pnpm test (59 passed, 19 skipped); pnpm test:browser (10 passed); pnpm test:postgres (19 passed); pnpm build; and git diff --check. The initial sandboxed unit run could not bind localhost (EPERM); the unrestricted rerun passed without code changes. No dependency, GitHub setting, Railway source, secret, IaC apply, production variable, deployment, or other remote state changed."
  - "On 2026-09-21 the owner explicitly approved next_slice_approval. The approved github-main-validation slice was archived in completed_slices and deployment-metadata-and-runbook became the current implementation slice. GitHub source connection, autodeploy, verifier implementation, and production variable changes remain out of scope."
  - "Fix Re-Review 08 verifies MEDIUM-02 as fixed and approves github-main-validation. The later approved deployment-metadata-and-runbook slice exists; explicit next_slice_approval is required before it begins. No application, CI workflow, IaC, dependency, GitHub setting, secret, or Railway state changed during re-review."
  - "The targeted MEDIUM-02 fix updates only the authoritative design's current status, GitHub baseline, public-visibility/credential boundary, slice routing, and the operator record's pre-activation SHA label. It requires a narrow design amendment and approval before railway-github-autodeploy chooses authenticated or anonymous verifier access. No application, CI workflow, IaC, dependency, GitHub setting, secret, or Railway state changed; MEDIUM-02 remains active pending targeted re-review."
  - "Implementation Review 07 accepts the CI workflow, active ruleset, and exact-SHA push validation but requires the documentation/state-only MEDIUM-02 fix: reconcile the authoritative design with the implemented public-repository state and require a narrow design amendment before the future verifier credential decision. No application, workflow, IaC, dependency, GitHub setting, secret, or Railway change is authorized."
  - "The github-main-validation slice is implemented and ready for implementation review. PR 1 passed Validate, active ruleset 23761397 requires PRs and strict validate while blocking deletion/force-push with no bypass, and merge commit 3e933bdb80eff22cb4edd5ab132dd979dea901cb passed the separate exact-SHA push Validate run 35587159315 with every required step successful. The single-owner capability exception uses zero approving reviews. No Railway resource or setting changed."
  - "On 2026-09-21 the owner made olegkirienko/guitar-mastering public and explicitly approved the github-main-validation operator activation. GitHub now permits rulesets. The repository has only the owner as collaborator, so the approved ownership-capability exception uses required pull requests with zero required approving reviews; requiring the author to obtain one approval would make compliant merge impossible. Strict validate, conversation resolution, deletion/force-push protection, and no bypass remain required."
  - "Node 24.7.0 local validation passed for the current slice changes: pnpm lint; pnpm test (55 passed, 19 skipped); pnpm test:browser (10 passed); pnpm test:postgres (19 passed); pnpm build; and git diff --check. The initial sandboxed pnpm test attempt could not bind localhost (EPERM); the unrestricted required rerun passed without code changes."
  - "The github-main-validation repository change is implemented locally: Validate now runs on pull requests and pushes to main, manual dispatch is removed, and acceptance coverage preserves the trigger/no-concurrency/validation-only invariants. Live GitHub read-back found only Validate active, but both rulesets and classic branch protection are unavailable for this private repository on its current plan (HTTP 403: upgrade to GitHub Pro or make the repository public). No push, visibility/plan change, policy mutation, or Railway operation was attempted. Exact-SHA push validation and the separately approved branch-policy mutation remain pending; see docs/operations/github-main-validation-plan.md."
  - "On 2026-09-21 the owner explicitly approved next_slice_approval. The approved railway-iac-parity-foundation slice was archived in completed_slices and github-main-validation became the current implementation slice. Railway source/autodeploy changes remain out of scope."
  - "Fix Re-Review 06 verifies MEDIUM-01 as fixed and approves railway-iac-parity-foundation. A later approved slice, github-main-validation, exists; explicit next_slice_approval is required before it begins. The approval supplied while the canonical phase was fix_rereview was not applied prospectively to this newly created gate."
  - "The targeted MEDIUM-01 fix reconciles only the authoritative design's current status/routing text and the operator record's malformed final sentence. It makes no IaC, application, dependency, test, or remote Railway change and is ready for targeted re-review."
  - "Implementation Review 05 accepts the IaC/runtime result but requires the documentation-only MEDIUM-01 fix: reconcile stale current-status/routing statements in the authoritative design and complete the malformed operator-record ending. No IaC, application, dependency, test, or remote Railway change is authorized."
  - "The amended foundation slice is implemented and ready for implementation review: explicit restart type removed from IaC, retry cap 3 retained, clean plans before and after railway.json removal, effective ON_FAILURE/3 and healthy topology verified read-only, local validation passed, no second apply or production mutation."
  - "Under the approved restart-policy exception, the IaC authoring source omits restartPolicyType and retains restartPolicyMaxRetries 3. A fresh production plan is clean; effective service and active deployment both report ON_FAILURE/3; production smoke, PostgreSQL, volume, domain, and PITR checks pass. The legacy railway.json file was then removed from the repository without a further IaC apply or production mutation."
  - "On 2026-09-20 the owner explicitly approved the Design Review 04 design_approval gate. The railway-iac-parity-foundation slice resumed under the restart-policy platform exception; implementation is limited to that approved slice."
  - "Design Review 04 approves the restart-policy parity amendment with no findings. The existing railway-iac-parity-foundation slice remains paused at design_approval; explicit owner approval is required before any IaC source edit or parity verification resumes."
  - "On 2026-09-20 a narrow restart-policy parity amendment was submitted: omit restartPolicyType from IaC, retain max retries 3, and verify effective ON_FAILURE/3 after deployment. The slice is paused for a new immutable design review and explicit design approval; no IaC edit, apply, railway.json removal, or production mutation is authorized by this transition."
  - "On 2026-09-20 the owner approved design_approval and explicitly accepted that Railway may build a deployment image before the pre-deploy exact-SHA verifier runs."
  - "On 2026-09-20 the owner approved the operator plan; the production Config File handoff and reviewed 0/2/0 IaC apply ran. The new web deployment is healthy, but the follow-up plan still proposes deploy.restartPolicyType, so parity is unproved. railway.json remains and implementation review is blocked; see docs/operations/railway-iac-parity-foundation-plan.md."
  - "Design Review 03 approved the revised design and closed HIGH-03 and MEDIUM-02."
  - "The first approved implementation slice is railway-iac-parity-foundation; design approval is required before it begins."
  - "No application code, GitHub settings, Railway configuration, secrets, or deployment was changed in this review phase."
