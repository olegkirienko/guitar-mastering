# Railway GitHub CI/CD and Infrastructure as Code

**Work item:** `railway-ci-cd-iac` (`infrastructure`)
**Status:** The `railway-iac-parity-foundation` slice was implemented on 2026-09-20 under the approved restart-policy amendment. Implementation Review 05 accepted the IaC and runtime result and requested only the `MEDIUM-01` documentation reconciliation now reflected here; targeted re-review is pending. No later slice or additional remote mutation is authorized before the required next-slice gate.

## Goal and boundaries

Make the release path PR → `Validate` → merge → `Validate` on the exact `main` commit → Railway GitHub autodeploy waiting for CI → build → pre-deploy exact-SHA proof → migration → readiness → live. Preserve the existing Railway production project, web service, PostgreSQL service, domain, variables, private database reference, production data, and PITR. Move deployment configuration from deprecated `railway.json` to `.railway/railway.ts` without making every application commit an infrastructure apply.

GitHub Actions remains validation only. It must not run `railway up`, production migrations, or IaC apply, and needs no Railway token for `Validate`. Infrastructure changes have a separate, reviewed operator path. No new service, database, domain, or bucket is part of this work item. One new production Railway secret, `GITHUB_ACTIONS_READ_TOKEN`, is required for the exact-SHA verifier because the repository is private; its creation and lifecycle are in the `railway-github-autodeploy` slice.

## Observed repository baseline

- `.github/workflows/ci.yml` names the workflow `Validate`, runs on `pull_request` and `workflow_dispatch`, has one `validate` job, no path filters, and no concurrency setting. It runs lint/typecheck, unit/foundation tests, browser tests, PostgreSQL integration tests against a temporary PostgreSQL 17 service, and `pnpm build`.
- `railway.json` supplies build `pnpm install --frozen-lockfile && pnpm build`; pre-deploy `pnpm db:migrate`; start `pnpm start`; readiness `/api/v1/readiness`; timeout `120` seconds; and `ON_FAILURE` with three retries.
- `server/config.ts` currently requires `DEPLOYMENT_VERSION`. `server/app.ts` emits it in structured request logs; health and readiness bodies contain status and request ID, not the version. Production cutover evidence records a stale static value. The current production runbook still describes a manual exact-source deploy.
- The last recorded production topology is project `112644ba-cb91-443b-ae4b-73a0d6f74b69`, environment `994fd373-dd1d-4073-8b7f-116e77d898fa`, web service `4d0a3739-0beb-4ea9-9a7e-7a9f3494708e`, PostgreSQL service `82d4b5e1-830a-4467-bb1f-f9448fd1d58d`, domain `1cff255c-4eea-493e-9c82-7d2ba1672046`, database volume `4495ab33-2440-4d29-a4d4-e214813676a8`, and PITR bucket `06fb8382-c35f-43be-b127-3fb98c30f3b0` ([record](../operations/legacy-platform-retirement-record.md)). These are historical evidence, so re-read their live identities and health before any later mutation.
- The request reports that production has no GitHub deployment source yet. This is an input assumption to verify read-only before implementation, not a claim established by this design phase.
- An authenticated `gh repo view olegkirienko/guitar-mastering --json visibility,nameWithOwner` on 2026-09-20 returned `PRIVATE`. The verifier cannot rely on anonymous Actions API access. Recheck visibility and token permissions immediately before credential creation.

## Verified platform behavior and design decisions

| Verified platform behavior | Project decision |
| --- | --- |
| Railway deploys pushes to the connected service branch; an existing service source can be changed in Settings or with `railway service source connect` ([services](https://docs.railway.com/services), [CLI](https://docs.railway.com/cli/service)). | Connect only the existing production web service to the existing repository's `main`; never create a substitute service. |
| Railway Wait for CI requires a GitHub workflow with `on.push.branches: [main]`. It evaluates completed **workflow conclusions**, not individual jobs or other apps' checks ([autodeploys](https://docs.railway.com/deployments/github-autodeploys)). | Keep one required, unconditional `Validate` workflow with one validation job. Review every other workflow triggered on `main` before enabling Wait for CI. |
| A failed workflow skips deployment; skipped/neutral never block; a cancelled workflow blocks only when no other workflow succeeded; unfinished workflows time out after two hours ([autodeploys](https://docs.railway.com/deployments/github-autodeploys)). | Railway Wait for CI alone cannot enforce success of this particular workflow. Add the fail-closed pre-deploy proof below before enabling autodeploy. Do not add skip conditions, path filters, or concurrency cancellation. |
| GitHub required status checks may count successful, skipped, or neutral conclusions as acceptable; skipped jobs can report success ([protected branches](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches), [status checks](https://docs.github.com/en/pull-requests/reference/status-checks)). | Branch protection is a PR gate, not proof the post-merge SHA passed. The post-merge `push` run and observed workflow conclusion supply that proof. |
| Railway IaC is CLI evaluated, uses `.railway/railway.ts`, and changes infrastructure only on `config apply`; `railway.json` overrides dashboard settings for legacy deployments and reaches a documented hard cutoff on 2026-12-01 ([IaC](https://docs.railway.com/infrastructure-as-code), [Config as Code](https://docs.railway.com/config-as-code)). | Migrate before cutoff. Keep app autodeploy and IaC apply separate. Do not enable the example automatic `railwayapp/config` apply workflow. |
| Railway pre-deploy runs after build, before app startup, within the private network; a nonzero exit prevents the deployment ([pre-deploy](https://docs.railway.com/deployments/pre-deploy-command)). Healthcheck success promotes the new deployment; timeout fails it, and healthchecks are not continuous monitoring ([healthchecks](https://docs.railway.com/deployments/healthchecks)). | Keep production migration in pre-deploy, keep database readiness probe, and monitor after promotion. |
| `RAILWAY_GIT_COMMIT_SHA` is provided for GitHub-triggered deployments ([variables](https://docs.railway.com/variables/reference)). | Use full SHA as the runtime deployment version in logs. Do not add a public version endpoint solely for this change; verify SHA via Railway deployment metadata and logs. |

## GitHub validation and branch policy

Add `push: { branches: [main] }` to the existing `Validate` workflow and remove `workflow_dispatch`. Retain `pull_request`, the single `validate` job, all validation commands, PostgreSQL service, and minimal permissions. Do not add branch/path filters to the `push` event or job-level `if` conditions. `pull_request` checks the PR merge candidate before merge; a merge, squash, or rebase can create a different SHA, so the `push` event validates the actual `main` commit Railway must deploy. Record `github.sha` and the workflow run URL for acceptance.

Leave `Validate` without workflow/job concurrency, including after adding `push`. GitHub's default allows runs in parallel. A concurrency group can cancel pending runs even with `cancel-in-progress: false`, and Railway's cancelled-run rule can allow another successful workflow to mask the cancellation ([GitHub syntax](https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax), [Railway autodeploys](https://docs.railway.com/deployments/github-autodeploys)). If resource pressure later demands serialization, design and review a mechanism that preserves every `main` commit's validation conclusion; do not add it implicitly here.

### Exact-SHA promotion guard (HIGH-01)

Before enabling production GitHub autodeploy, put a small TypeScript/Node verifier at the **start** of Railway pre-deploy, followed by `pnpm db:migrate` only on verifier success. Implement it in `server/verify-ci.ts`, which the existing `tsconfig.server.build.json` compiles to `dist-server/verify-ci.js`; add a `release:predeploy` package script with the exact command `node dist-server/verify-ci.js && pnpm db:migrate`, and set Railway's effective pre-deploy command to `pnpm release:predeploy`. Verify the built image contains that JavaScript file and package script. The verifier uses `RAILWAY_GIT_COMMIT_SHA` and a pinned `olegkirienko/guitar-mastering` repository and `.github/workflows/ci.yml` identity to query the GitHub Actions API. It requires a completed `success` workflow run with `event: push`, `head_branch: main`, and `head_sha` equal to the full deployment SHA. It then requires the single `validate` job and each named validation step (install, lint, unit, browser, PostgreSQL, build) to have `success` conclusions. It rejects absent or malformed SHA, no matching run, skipped/neutral/cancelled/failing jobs or steps, API errors, pagination uncertainty, and timeouts. Test the parser with representative API fixtures and a live read-only call before enabling. The guard must have a short bounded timeout and a clear nonsecret diagnostic. If GitHub cannot be reached, the new deployment fails before migration and startup. The exact bootstrap order and production proof are specified below.

Set a Railway production variable `GITHUB_CI_GATE_REQUIRED=true` before source connection. With this flag, the verifier rejects deployments without Railway Git SHA metadata, including manual CLI releases; emergency manual release requires a separately approved, time-bounded flag change and read-back. The old healthy deployment remains serving while a new pre-deploy fails. Only GitHub-triggered commits are within the automated release invariant. Railway may still fetch and build a SHA before the verifier runs, so the preventive guarantee is **no migration, startup, or promotion without the exact successful push validation**; the target sequence's strict “CI before build” property remains dependent on Railway Wait for CI semantics. The design-approval gate must explicitly accept that residual build risk; otherwise keep autodeploy disabled and choose a different deployment architecture. A commit that modifies the verifier or workflow needs human review under branch rules.

### GitHub API credential (MEDIUM-02)

The repository is private, and GitHub's [workflow runs](https://docs.github.com/en/rest/actions/workflow-runs) and [jobs](https://docs.github.com/en/rest/actions/workflow-jobs) endpoints require `Actions: read` for private repository access. The repository owner creates a fine-grained personal access token restricted to **only** `olegkirienko/guitar-mastering`, with `Actions: read` and no write permission, a recorded expiry of at most 90 days, and explicit approval of that exact credential scope. Store it as a sealed production web-service Railway variable named `GITHUB_ACTIONS_READ_TOKEN`; the IaC file references it with `preserve()` and never contains its value. Do not copy it into GitHub Actions, source, plans, logs, tickets, or evidence. The verifier sends it only to `api.github.com` over HTTPS and reports only status class and request outcome. Confirm the token can read both endpoints, inspect rate-limit headers under expected release frequency, and fail closed on 401, 403, 429, exhaustion, or network error. The owner records the expiry privately, rotates it at least 14 days before expiry using a new equally scoped token and a reviewed Railway variable change, verifies a read-only API call, then revokes the old token. On compromise, disable autodeploy, revoke the token, install a replacement, and recheck the guard before re-enabling. Token creation, storage, rotation, and revocation are production credential changes requiring an explicit reviewed operator plan within slice 4; the design phase creates no token.

Before enabling Wait for CI, inventory enabled GitHub Actions workflows, reusable calls, and GitHub Apps producing checks on `main`. Keep `Validate` as the sole success-producing Actions workflow on production commits, or document every additional one and prove it cannot mask a cancelled `Validate`. Remove `workflow_dispatch` from `Validate`; a manual rerun or dispatch is not accepted as the required push run. A separate diagnostic workflow may be designed later, but it must not create a successful check suite on the deployment SHA while Wait for CI is active. Audit workflow triggers and branch rules again before every release-policy change. Disallow CI skip directives in PR title, commit subjects, merge/squash messages, and release procedure; require the unskipped post-merge push run in the guard. GitHub can still omit that run after a skip directive, and Railway may proceed on a skipped/neutral or masked cancelled result; the guard then rejects it. A successful push workflow with skipped required steps also fails the guard. Branch protection alone cannot prevent those cases ([GitHub skip rules](https://docs.github.com/en/actions/how-tos/manage-workflow-runs/skip-workflow-runs), [Railway Wait for CI](https://docs.railway.com/deployments/github-autodeploys)).

Desired active `main` ruleset or equivalent branch protection: require PRs (at least one human approval and resolved conversations, subject to repository ownership capability), require the unique `validate` GitHub Actions check from `Validate`, prohibit force push/deletion, restrict or eliminate direct push and admin bypass where the repository plan permits, and prevent workflow-file edits from bypassing review. Check current repository ownership and plan features before choosing the exact settings. Require current branch state before merge or use merge queue only with a separately verified `merge_group` trigger; merge queue is outside this slice. Manual workflow runs cannot substitute for the required `main` push run. Document that GitHub's required-check policy guards merge while Railway Wait for CI controls build timing and the pre-deploy verifier guards migration and promotion; acceptance correlates SHAs and run conclusions.

## Railway IaC handoff and parity

Railway now documents `railway config migrate`, which is safer than a hand-written import for this legacy Config as Code service. A critical constraint is that `config plan` refuses to manage a service while its Config as Code association remains active. `config migrate` previews generated code; `config migrate --apply` writes IaC and clears Railway Config File settings, but does **not** itself apply the IaC graph. `--delete-files` is optional and must not be used during the handoff ([IaC migration](https://docs.railway.com/infrastructure-as-code), [config CLI](https://docs.railway.com/cli/config)).

The controlled handoff and its first reviewed apply ran on 2026-09-20. The approved restart-policy exception was then validated with a clean plan and effective `ON_FAILURE`/three-retry read-back before `railway.json` was removed. The numbered sequence below records that completed procedure; it does not direct a repeat apply. See the [operator plan](../operations/railway-iac-parity-foundation-plan.md) for execution evidence.

1. Read-only preflight: verify exact project/environment/service IDs, current deployment and config source, all six `railway.json` behaviors, GitHub source state, production domain, database reference/private host, variable **names and references** without publishing values, database volume/PITR health, and production smoke. Capture the effective deployment configuration and a restore plan. Do not use `config pull --include-variables` or print decrypted values.
2. Run `railway config migrate` without `--apply` to preview the generated `.railway/railway.ts`. If needed, use `config pull` to compare existing Railway state. Install/pin the documented `railway` TypeScript SDK with pnpm only when implementation needs it. Confirm generated code targets the exact existing web service; preserve existing variables with `preserve()`, existing domain, PostgreSQL service, volume, and PITR. Treat any proposed add/delete or topology change as a blocker.
3. Review the parity table below against live effective settings. Keep `railway.json` in the repository and in the rollback record. Perform `config migrate --apply` **without `--delete-files`** only in a separately approved implementation slice and controlled window. This clears the Config File association, which is a Railway configuration mutation. Immediately run `config plan` against production. If the plan is not limited to intended web-service setting transfer, stop and restore the prior Config File association; do not apply the IaC graph.
4. When plan matches the reviewed transfer, execute an explicitly reviewed `config apply` against the exact production environment; then read back effective settings and health. A clean follow-up plan is the drift baseline. Remove `railway.json` only after this parity proof and after confirming future deployments use the IaC-managed settings. Commit/file removal and Railway association clearance must be coordinated so no interim deploy runs with missing settings. No autodeploy source is connected during this handoff.

| Behavior | Required effective value after handoff |
| --- | --- |
| Build | `pnpm install --frozen-lockfile && pnpm build` |
| Pre-deploy at initial IaC handoff | `pnpm db:migrate`; slice 4 later changes it to `pnpm release:predeploy` after the verifier artifact is live |
| Start | `pnpm start` |
| Readiness path | `/api/v1/readiness` |
| Healthcheck timeout | `120` seconds |
| Restart | `ON_FAILURE`, maximum `3` retries |

### Restart-policy platform exception and parity rule

Railway documents `ON_FAILURE` as the effective default. After the reviewed production IaC apply, the active deployment and service instance report `ON_FAILURE` with `restartPolicyMaxRetries = 3`, but fresh raw configuration reads omit `restartPolicyType`. Railway IaC is stateless, and SDK 3.11.0 accepts the explicit field in the desired graph; nevertheless, a fresh plan repeatedly proposes `restartPolicyType: null → "ON_FAILURE"`. The explicit default does not round-trip through the current raw/IaC state model. This is a Railway platform exception, not proof that the effective restart behavior is wrong.

The IaC source shall omit `deploy.restartPolicyType` and continue to manage `deploy.restartPolicyMaxRetries: 3`. For this project, parity means a clean IaC plan for every supported round-trippable field, together with documented platform exceptions validated by runtime/read-only checks. After each deployment, read the effective service/deployment restart policy and require type `ON_FAILURE` and maximum retries `3`; stop acceptance and investigate if either differs or cannot be verified. Record the exception and the check alongside the plan. If Railway later exposes `restartPolicyType` as stable round-trippable state, a reviewed design and IaC change may restore explicit management.

The IaC source may need to represent the existing PostgreSQL resource because a whole-project graph can treat omitted owned resources as deletion. Use the importer/preview and plan to establish ownership semantics. Do not claim a no-op plan is possible until the Config as Code association is cleared. Any inability to preserve effective healthcheck or restart behavior is a design-review blocker for the migration; the restart type exception above requires its own read-only proof. Generated domains are omitted by Railway's importer; preserve and read back the existing domain rather than recreating it.

IaC drift detection: an operator runs `railway config plan --detailed-exit-code` against the pinned production target during a reviewed maintenance check and before every proposed IaC apply; `0` means no drift in supported round-trippable fields, `2` means pending changes, other nonzero is an error. The restart-type exception needs the separate effective-policy check above. Capture redacted output only. Infrastructure changes require a reviewed PR changing `.railway/railway.ts`, explicit operator approval of the exact plan, then a manual `config apply` with fresh read-back. A stale plan or unexpected resource deletion stops the operation. Do not wire `config apply` into `Validate` or an unconditional `main` push workflow. Normal `main` commits only drive application autodeploy.

Rollback before IaC apply: restore the previous Railway Config File association and keep `railway.json`, verify the previous production deployment and readiness. Rollback after apply: use a reviewed inverse IaC change or restore the previous settings/association from the captured snapshot; re-plan and read back identities and behavior. If an app deploy occurs during a bad configuration window, disable autodeploy and restore the prior migration-compatible deployment. Never restore the production database or recreate PostgreSQL merely to reverse IaC configuration.

## Source connection and first-link safety

### Guard bootstrap on the existing production image (HIGH-03)

Railway [pre-deploy](https://docs.railway.com/deployments/pre-deploy-command) runs inside the image built for a deployment, so applying a command that names a file absent from that image is unsafe. Railway [variables](https://docs.railway.com/variables) create staged changes; deploying [staged changes](https://docs.railway.com/deployments/staged-changes) can redeploy affected services. The exact side effect of `config apply` and the current service's staged-change state must be verified read-only immediately before each production mutation. Treat every configuration apply or variable activation as capable of creating a deployment, record the prior successful deployment ID, watch history from before the action through completion, and stop if an unplanned deployment appears. Never presume a configuration write is inert.

Use this ordered bootstrap while the production service has **no GitHub source or autodeploy**:

1. In slice 4, implement and test `server/verify-ci.ts` and `release:predeploy`; merge the reviewed change into `main`. At this point the effective Railway pre-deploy command is still `pnpm db:migrate`, and `GITHUB_CI_GATE_REQUIRED` is absent. Perform one separately reviewed **CLI deployment of that exact source commit** to the existing web service under the old command. Observe its own deployment ID reach `SUCCESS`, readiness and smoke pass, and the prior deployment remain available. In the active image, use a read-only Railway SSH command to verify `dist-server/verify-ci.js` and the package script are present and executable. Record the source SHA, deployment ID, and artifact path. Stop if the active image cannot be proved to contain the verifier.
2. The repository owner creates the approved GitHub token described above and stores it as `GITHUB_ACTIONS_READ_TOKEN` on the production web service. Review and activate any staged variable change while the **old pre-deploy command** is effective. If Railway redeploys, it uses the already verified image and old migration command; observe that exact deployment and smoke before continuing. Confirm the token variable **name** is present without displaying its value, and use read-only execution inside the current production image to prove it can query the pinned workflow runs and jobs APIs. If the token is unavailable in the active image, complete a controlled, reviewed deployment under the old command to activate it. Stop if the API proof fails.
3. Set `GITHUB_CI_GATE_REQUIRED=false` explicitly for bootstrap, then apply a separately reviewed IaC plan changing **only** the web pre-deploy command to `pnpm release:predeploy` plus that flag and any intended preserved token reference. Check the saved plan, source tree, service ID, and no add/delete before apply. If this config or flag activation creates a deployment, its image already contains the verifier and the explicit `false` mode allows migration; observe its `SUCCESS`, readiness, and smoke. Read back the effective command and flag, verify there are no pending staged changes, and inspect deployment history. The verifier must bypass only on the literal `false` value; missing or any other value fails closed. If read-back cannot establish the candidate deployment's effective settings, stop.
4. On the verified active production image, invoke **only** `node dist-server/verify-ci.js` through a read-only Railway SSH command with `GITHUB_CI_GATE_REQUIRED=true` temporarily in that command's process environment. Supply a known successful `main` push SHA to prove the authenticated positive API path, then an impossible full-length SHA to prove a nonzero, nonsecret failure; neither invocation runs `pnpm db:migrate` or modifies the running app. Capture exit codes and redacted diagnostics. SSH/container execution is mandatory for this proof; a local `railway run` is insufficient because it does not prove the deployed image. If SSH or an equivalent image-local read-only execution is unavailable, stop before source connection.
5. Change the production flag to `true` under a separate reviewed action. Prefer a supported commit of staged configuration without redeploy; if activation creates a CLI-source deployment with no `RAILWAY_GIT_COMMIT_SHA`, its guard must fail **before migration**, and the previously healthy deployment must continue serving. Record that exact failed deployment ID and confirm no migration log for it. Read back the effective flag and pre-deploy command for **future** deployments, verify no pending staged change, inspect deployment history, and run readiness/smoke against the prior healthy deployment. If the flag is not active for a new deployment, an unexpected deployment promotes, or the previous deployment is not healthy, stop and contain before source connection. Do not temporarily set `false` again merely to make a CLI deployment succeed.

If any earlier configuration action creates an unplanned deployment, freeze configuration/source changes, identify its ID and image, and inspect pre-deploy logs. Cancel it before promotion if supported and safe; otherwise wait for a terminal result, verify the prior healthy deployment and smoke, and restore reviewed settings if necessary. Resume only after a new reviewable plan explains the behavior. A failed CLI deployment in step 5 is an anticipated negative guard check only when it demonstrably runs the verifier before migration and leaves the old deployment healthy.

### First source link (HIGH-02)

Before **any** production source mutation, land and verify the `push main` workflow on the current `main` SHA, establish the `main` ruleset, inventory checks, and prove the source tree no longer depends on `railway.json`. Record a healthy production deployment, the reviewed full `main` SHA, all resource identities, and smoke results. Confirm Railway GitHub App permissions and a linked member with contributor access. Complete the production-image guard bootstrap above while autodeploy is still absent. Its required flag must already be true, and the image-local positive and negative tests, effective command, token access, and old-deployment smoke must be recorded.

Make a **read-only first-link proof** a hard entry criterion. Obtain current Railway documentation, support confirmation, or an isolated same-feature rehearsal establishing exactly whether source connection itself creates a deployment, whether the existing service can have autodeploy disabled *before* connection, and whether Wait for CI can be enabled before the first deploy. Capture the evidence, CLI/UI version, and exact setting order in the slice's reviewable operator plan. The public [source-connect CLI](https://docs.railway.com/cli/service) documents `--repo` and `--branch` but no no-deploy flag; [autodeploy docs](https://docs.railway.com/deployments/github-autodeploys) say a connected service deploys branch pushes and document a Disable control, but do not prove first-link atomicity. Thus **do not invoke** `railway service source connect` or select the production repo in the UI on those docs alone. If a safe order cannot be proved before the action, stop this slice with autodeploy disabled and request a design amendment. A disposable rehearsal is optional evidence only; it must not change the existing production service or database.

Once proof establishes a supported order, follow only that recorded sequence on the existing web service: disable autodeploy before connection if supported; connect `olegkirienko/guitar-mastering` branch `main`; keep it disabled; read back source, service/environment IDs, domain, variable references, database/PITR identities, and deployment history; enable Wait for CI; read it back; enable autodeploy; read it back. If the supported order instead permits Wait for CI before connection, enable and verify it first. Do not proceed if neither order rules out an ungated deploy. Freeze pushes during the sequence. The first automatically deployed commit must be a later controlled `main` commit with a fresh, actual `Validate` push success. Keep Railway watch paths empty.

If a deployment unexpectedly appears at connection, immediately disable autodeploy and stop further source or IaC changes. Identify and monitor that exact deployment; if it is waiting, cancel it using a verified supported action before build/pre-deploy; if it has started, the required guard must fail before migration unless the SHA has a successful push `Validate`. If promotion occurred, restore the previously recorded migration-compatible deployment through a reviewed Railway deployment action. Confirm the prior deployment ID is serving with readiness and smoke, and inspect migration logs before considering any retry. Preserve the same web service, PostgreSQL, volume, domain, and PITR identities throughout. A surprise deployment is a failed slice requiring incident evidence and renewed review, even if smoke remains healthy.

Expected state: a new `main` SHA is seen by Railway → deployment `WAITING` → all GitHub Actions check suites conclude → failed workflow means skipped deployment and old healthy release remains; successful workflows allow build; the pre-deploy verifier independently requires exact-SHA successful push validation before migration or promotion. Skipped/neutral never blocks Railway, cancelled is conditional, and two-hour timeout skips. If the verifier or effective pre-deploy command cannot enforce the invariant for the repository's actual workflow set, keep autodeploy disabled and revisit the integration design.

## Version and migration lifecycle

Change runtime version resolution to prefer `RAILWAY_GIT_COMMIT_SHA` for GitHub deployments. During transition, retain `DEPLOYMENT_VERSION` as a fallback for current CLI-built releases and local/test fixtures. After a successful GitHub deploy proves full-SHA logging, retire the production static `DEPLOYMENT_VERSION` variable in a separately reviewed configuration change; local/test may use an explicit value. Do not make missing Railway Git metadata crash a rollback image. Keep health/readiness payloads stable and unauthenticated; expose the SHA in startup/structured logs and correlate it with Railway deployment metadata. A public version endpoint is unnecessary for this work item.

CI's PostgreSQL tests validate migrations against a temporary database. Production `pnpm db:migrate` remains Railway pre-deploy, using the existing private PostgreSQL reference. A failed migration stops that deployment before app startup; inspect logs, fix forward, and assess any partial nontransactional schema change before retry. Readiness timeout fails the new deployment; Railway retains the previous active deployment when healthcheck promotion fails, but the database may already have changed, so schema changes must remain backward compatible. After promotion, healthcheck is not continuous monitoring: run production smoke and inspect logs/metrics. Roll back application image only to a schema-compatible release; use PITR only for an independently reviewed database recovery incident.

## Acceptance and evidence

Positive controlled release: open PR with an inert observable change; capture PR `Validate` success, merge, capture new `main` SHA and its separate `push` `Validate` run, observe Railway `WAITING`, then workflow success and deployment of the **same full SHA**. Verify the pre-deploy verifier's success before migration, migration log success, new deployment `SUCCESS`, readiness 200, production smoke, structured SHA logs, service/domain/database/PITR identities, and absence of unexpected IaC apply. Include the bootstrap CLI deployment ID, effective command/flag read-back, image-local positive and negative guard results, the token's restricted permission/expiry metadata (never its value), and the first-link proof. Save run/deployment URLs, UTC times, SHAs, redacted plan/read-back, smoke results, and metrics in an operations evidence artifact.

Negative controlled test (MEDIUM-01): prepare a reviewed PR that adds an application-inert `Validate` step whose failure condition is exactly `github.event_name == 'push' && github.ref == 'refs/heads/main'` **and** a temporary fixture marker in the repository. The step exits nonzero only when both conditions hold. PR `Validate` runs every ordinary command and succeeds because its event is `pull_request`; branch protection and one human approval remain active. Merge with no skip directive. Capture the resulting full `main` SHA, the actual `push` `Validate` run URL and failed fixture step, and Railway's deployment record for that same SHA in `WAITING` then `SKIPPED` due to CI failure (including Show Skipped). Prove no build, pre-deploy migration, or promotion for that SHA; record the unchanged prior successful deployment ID and production readiness/smoke during and after the failure. A PR-only failure is additional branch-rule evidence, not a substitute. Prepare and review the recovery PR in advance: it removes the marker and temporary step, passes PR validation, and merges normally; its distinct `main` SHA must pass push `Validate`, the pre-deploy verifier, migration, and smoke. Capture both SHAs and UTC times. No application behavior or database migration is changed by the failure fixture. Do not bypass branch protection or push directly.

If the fixture cannot be run safely under the actual protected-branch settings, use an isolated Railway environment with the same Wait for CI setting to collect provider behavior and collect production guard unit/integration evidence, but mark production negative acceptance **unproven**. The work item cannot transition to complete on that evidence alone: record an explicit unresolved acceptance decision at the final human gate, or revise the design and gain approval for an alternative supported proof. Also inspect skipped, neutral, cancelled, and timed-out conclusions by documented rule plus safe nonproduction evidence where available.

Implementation must update `docs/operations/production-cutover.md`, README contributor guidance, and a CI/CD + Railway operations runbook with the canonical release path, manual IaC plan/apply and drift procedure, SHA correlation, CI failure/timeout diagnosis, migration/readiness failure response, and rollback. Record production IDs as references to be rechecked, not as a reason to recreate resources.

## Approved implementation slices proposed for review

1. **`railway-iac-parity-foundation`** — Capture live parity; generate and review IaC; perform the controlled Config as Code association handoff and reviewed apply only after zero unexpected changes; read back a clean plan for supported round-trippable fields and verify documented platform exceptions through effective runtime/read-only checks before removing `railway.json`. Keep `restartPolicyMaxRetries: 3` in IaC and verify effective `ON_FAILURE` with three retries after deployment. No GitHub source/autodeploy. The slice is implemented; Implementation Review 05 accepted its IaC/runtime result and routed only the documentation reconciliation to targeted fix and re-review. Slice 2 remains unauthorized until that re-review approves the slice and the owner accepts `next_slice_approval`.
2. **`github-main-validation`** — Add `push main` without losing coverage; remove `workflow_dispatch` from `Validate`; verify exact-SHA run and branch policy; configure the reviewed ruleset under its own explicit operator approval. No Railway source connection. Gate on successful `main` validation and no cancellation risk.
3. **`deployment-metadata-and-runbook`** — Introduce SHA-backed logs with safe fallback; update release, IaC, and contributor documentation. Keep existing production static variable until a GitHub-triggered deployment proves metadata. No source connection.
4. **`railway-github-autodeploy`** — Implement and test the exact-SHA verifier, provision the narrowly scoped GitHub read token under explicit operator approval, perform the ordered production-image guard bootstrap and its reviewed CLI deployment/IaC/variable changes, and prove positive and negative verifier outcomes in the deployed image. Obtain first-link behavior proof *before* connecting the existing production web service. Follow only the proved safe sequence to enable Wait for CI and autodeploy; otherwise stop for design amendment. Preserve identities and record settings. No schema mutation beyond normal deployment pre-deploy.
5. **`end-to-end-cicd-acceptance`** — Run positive and protected-branch negative acceptance; correlate exact SHA and all gate outcomes; smoke and observe production; retire static production version variable only after proven Git metadata; record evidence and close runbook gaps. Do not weaken branch rules for the negative test; unresolved production failure-path evidence blocks completion.

Each slice is bounded by the repository's design review, human approval, implementation review, and next-slice/completion gates. Remote source connection, branch rules, IaC handoff/apply, token provisioning, and production acceptance require explicit reviewable plans within their slices. Targeted re-review of `MEDIUM-01` is the next workflow action; if it approves slice 1, explicit `next_slice_approval` is required before `github-main-validation` begins.
